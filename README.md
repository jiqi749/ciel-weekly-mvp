# Ciel 时间记录日志

一个纯静态的个人时间记录网页。当前版本不需要后端，数据保存在浏览器本地 `localStorage`。

## 静态部署

这个文件夹可以直接部署到任意静态网站托管服务：

- GitHub Pages
- Netlify
- Vercel
- Cloudflare Pages

入口文件是 `index.html`。

## 注意

当前数据只保存在当前浏览器里。换设备或清理浏览器数据后，记录不会自动同步。
